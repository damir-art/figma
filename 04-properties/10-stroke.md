# Stroke (обводка)
Обводка применяется ко всем фигурам (шейпам).

* Кликаем по фигуре `Design > Stroke`
* округление углов обводки работает только при `Outside` и `Center`
* `Dashes` - обводка отрезками (штрихами), `12,5`
* `Cap` работает со штриховой обводкой